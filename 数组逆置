void Reverse(int a[],int l,int r)//参数是一个数组以及要逆置的数组范围的左右下标，返回类型为空
{
  int i,j,tep;
  for(i=l,j=r;i<j;i++,j++)      //要注意i，j，的运用，并不是参数传进来直接对参数进行操作，而是选择一个适当的傀儡来充当当事人，以完成计数记次的工作
  {
    tep=a[i];
    a[i]=a[j];
    a[j]=tep;
  }
}
